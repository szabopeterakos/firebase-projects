import * as ActionName2Actions from './action-name2.actions';

describe('ActionName2', () => {
  it('should create an instance', () => {
    expect(new ActionName2Actions.LoadActionName2s()).toBeTruthy();
  });
});
